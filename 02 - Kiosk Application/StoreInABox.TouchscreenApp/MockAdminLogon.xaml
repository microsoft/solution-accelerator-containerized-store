<Page
    x:Class="StoreInABox.TouchscreenApp.MockAdminLogon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StoreInABox.TouchscreenApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Button Style="{StaticResource ImageButton}" Click="Back_Clicked" Height="60" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,20,0,0" Background="{x:Null}">
            <Image Source="Assets/btn_back_Up.png"/>
        </Button>

        <StackPanel Orientation="Vertical" Width="300" HorizontalAlignment="Left" Margin="152,161,0,0">
            <TextBox x:Name="txtUserName" PlaceholderText="UserName"/>
            <PasswordBox x:Name="pwbPassword" PlaceholderText="Password"/>
            <Button Content="Log In" Click="Login_Clicked"/>
        </StackPanel>
    </Grid>
</Page>
